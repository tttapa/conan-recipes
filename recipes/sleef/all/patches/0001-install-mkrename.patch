diff --git a/src/libm/CMakeLists.txt b/src/libm/CMakeLists.txt
index 25e1177..534aaa9 100644
--- a/src/libm/CMakeLists.txt
+++ b/src/libm/CMakeLists.txt
@@ -1054,6 +1054,13 @@ install(
     COMPONENT sleef_Development
 )
 
+if (NOT CMAKE_CROSSCOMPILING)
+    install(
+        TARGETS ${TARGET_MKRENAME} ${TARGET_MKALIAS} ${TARGET_MKDISP}
+        RUNTIME DESTINATION "${CMAKE_INSTALL_BINDIR}"
+        COMPONENT sleef_mkrename)
+endif()
+
 if(ENABLE_GNUABI)
   install(
       TARGETS ${TARGET_LIBSLEEFGNUABI}
@@ -1078,4 +1085,10 @@ if(ENABLE_GNUABI)
       COMPONENT sleef_Development
       NAMELINK_ONLY
   )
+
+    if (NOT CMAKE_CROSSCOMPILING)
+        install(TARGETS ${TARGET_MKRENAME_GNUABI} ${TARGET_MKMASKED_GNUABI}
+            RUNTIME DESTINATION "${CMAKE_INSTALL_BINDIR}"
+            COMPONENT sleef_mkrename)
+    endif()
 endif()
